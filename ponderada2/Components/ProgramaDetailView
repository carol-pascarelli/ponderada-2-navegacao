import SwiftUI

struct ProgramaDetailView: View {
    
    let programa: Programa
    
    var body: some View {
        
        ScrollView {
            InfoBagde(programa: programa)
        }
        .ignoresSafeArea(edges: .top)
        .navigationBarTitleDisplayMode(.inline)
    }
}
